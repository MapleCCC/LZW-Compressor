language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install -r requirements-test.txt
  - pip install -r requirements-ci.txt
script:
  - pytest --cov=LZW test/
after_success:
  - codecov
